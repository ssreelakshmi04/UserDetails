version: '3.8'

services:
  userdetails:
    build: .
    container_name: userdetails
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
    restart: unless-stopped
    volumes:
      - .:/app
